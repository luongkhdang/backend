1.  **Reconcile Influence Logic:**
    - **(Implement):** Implement the influence calculation in `influence.py` to utilize the `entity_type_weights` and `entity_influence_factors` tables as intended.
